# application-local.yml
server:
  port: 8096
  error:
    include-message: always
    include-binding-errors: always

spring:
  application:
    name: rutix

  rabbitmq:
    host: localhost        # ← Cambiado de 'rabbitmq' a 'localhost'
    port: 5672
    username: guest
    password: guest

  boot:
    admin:
      client:
        url: http://localhost:8086   # ← Cambiado de 'ms-admin'

  datasource:
    url: jdbc:mariadb://localhost:3307/mep  # ← Cambiado de 'mariadb'
    driverClassName: org.mariadb.jdbc.Driver
    platform: org.hibernate.dialect.MariaDBDialect
    username: elementarystudio
    password: YXFzd2RlZnI=

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MariaDBDialect
    properties:
      hibernate:
        show_sql: true
        use_sql_comments: false
        format_sql: false
        ddl-auto: update

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8082/eureka/  # ← Cambiado de 'ms-eureka'
  instance:
    prefer-ip-address: true

Feign:
  hystrix:
    enabled: true

management:
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans  # ← Cambiado de 'zipkin'
  metrics:
    tags:
      application: ms-tokens-materiales
    export:
      prometheus:
        enabled: true
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: info, health, prometheus, metrics
